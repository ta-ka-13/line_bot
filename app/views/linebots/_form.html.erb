<%= form_with(model: linebot) do |form| %>
  <% if linebot.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(linebot.errors.count, "error") %> prohibited this linebot from being saved:</h2>

      <ul>
        <% linebot.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
